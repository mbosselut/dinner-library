<!DOCTYPE html>

<html>
    <head>
        <title>Recipe Library</title>
        <meta name="viewport" content="width=device-width,initial-scale=1.0">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    <body>

    <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" id="page-title" href="/recipes">Dinner Library</a>
                </div>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav navbar-right">
                        <% if(!currentUser){ %>
                            <li id="nav-links"><a href="/login">Login</a></li>
                            <li id="nav-links"><a href="/register">Sign Up</a></li>
                        <% } else { %>
                            <li id="nav-links"><a href="">Signed in as : <%= currentUser.username %></a></li>
                            <li id="nav-links"><a href="/logout">Logout</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>

    <div class="container">
        <!-- error isnt undefined cause it's an empty array by default, you need to check length to confirm existence -->
        <% if(error && error.length > 0){ %>
            <div class="alert alert-danger" role="alert"><%= error %></div>
        <% } %>
        <% if (success && success.length > 0){ %>
            <div class="alert alert-success" role="alert"><%= success %></div>
        <% } %>
    </div>
    
